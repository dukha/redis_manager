<p class="pageheading"><%=t("headings.edit.heading", :model=>t($ARM + "whiteboard_type.one") )%></p>

<%= render 'form' %>

<%= link_to t('headings.new.heading', :model=> t($ARM + 'whiteboard_type.one')),  new_whiteboard_type_path %> |
<%= link_to t("actions.back"), whiteboard_types_path %>
