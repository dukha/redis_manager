<p class="pageheading"><%=t("headings.edit.heading", :model=>t($ARM + "redis_database", :count=>1) )%></p>


<%= semantic_form_for @upload, :url => { :action => "select_translation_to_redis", :controller => "uploads"  } do |f| %>
  <%=semantic_fields_for @redis_database do |t| %>>
    <% t.inputs do  %>
      <%= t.input :id, :as=>:select,:collection=> RedisDatabase.all.collect {|t| [ t.name, t.id ]} %>
    <% end %>
  <%end%>
<%end%>

          

<%= link_to t('headings.new.heading', :model=> t($ARM + 'redis_database', :count=>1)), redis_databases_path %> |
<%= link_to t("actions.back"), redis_databases_path %>
