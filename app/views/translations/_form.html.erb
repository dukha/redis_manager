<%= semantic_form_for(@translation) do |f| %>
  
  
	<% f.inputs do %>
           <%= f.input :application_version_id, :as=>:select,:collection=> ApplicationVersion.all.collect {|p| [ p.application_name_with_version, p.id ]} %>
				   <%= f.input :redis_db_index  %>
				     
				   <%= f.input :host  %>
           <%= f.input :port  %>
		
	<% end %>


	<%= f.buttons do %>
      <%= f.commit_button  %>    
  <% end %>
<% end %>

